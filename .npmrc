# Hoist Prisma engines so CLI and client resolve consistently with pnpm's isolated node_modules.
public-hoist-pattern[]=*prisma*
public-hoist-pattern[]=@prisma/*

auto-install-peers=true

# Avoid Windows issues with `cloudflare:sockets` paths during bundling.
node-linker=hoisted
